/**
 * Test Attempt Layout — Minimal, distraction-free.
 * No Navbar, no sidebar. Just the test.
 */
export default function TestAttemptLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
